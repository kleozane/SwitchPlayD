@model IEnumerable<SwitchPlayD.Data.Studio>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            <div class="card strpied-tabled-with-hover">
                <div class="card-header">
                    <h4 class="card-title">Studio List</h4>
                </div>
                <div class="card-body table-full-width table-responsive">
                    <div class="row">
                        <div class="col-md-6">
                        </div>
                        <div class="col-md-6">
                            <a asp-action="Create" asp-controller="Studio" class="btn btn-primary float-right"><i class="fa-solid fa-plus"></i> Create</a>
                        </div>
                    </div>
                    <br />
                    <table class="table table-hover table-striped">
                        <thead>
                            <tr>
                                <th>Logo</th>
                                <th>Name</th>
                                <th>Description</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Logo</td>
                                    <td>@item.Name</td>
                                    <td>@item.Description</td>
                                    <td class="float-right">
                                        <a class="text-warning" title="Edit" asp-route-id="@item.Id" asp-action="Edit"><i class="fa-solid fa-pen-to-square"></i></a>
                                        &nbsp;
                                        <a class="text-danger" title="Delete" asp-route-id="@item.Id" asp-action="Delete"><i class="fa-solid fa-trash"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <div class="col-md-1"></div>
    </div>
</div>
